# Known Issues

- `.gitignore` and `source/locale/**`: compiled `.mo` files are tracked even though `I18N_GUIDE.md` and `IMPLEMENTATION_SUMMARY.md` say they should be excluded; `.gitignore` also lacks an entry for `*.mo`.

